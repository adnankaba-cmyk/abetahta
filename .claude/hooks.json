{
  "hooks": {
    "postToolUse": [
      {
        "matcher": "Write|Edit",
        "command": "(cd /d/AbeTahta/packages/web && npx tsc --noEmit 2>&1 | grep 'error TS' | head -5); (cd /d/AbeTahta/packages/server && npx tsc --noEmit 2>&1 | grep 'error TS' | head -5)"
      }
    ],
    "sessionStart": [
      {
        "command": "cat /d/AbeTahta/PROJE_DURUMU.md 2>/dev/null | head -50"
      }
    ]
  }
}
